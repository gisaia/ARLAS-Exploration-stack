services:
  arlas-persistence-server:
    extends:
      file: ref-dc-arlas-persistence-server.yaml
      service: arlas-persistence-server
    depends_on:
      db:
        condition: service_healthy
      auth-server:
        condition: service_healthy
    environment:
        - ARLAS_AUTH_POLICY_CLASS="${ARLAS_AUTH_POLICY_CLASS}"
        - ARLAS_AUTH_PERMISSION_URL=${ARLAS_AUTH_PERMISSION_URL}
