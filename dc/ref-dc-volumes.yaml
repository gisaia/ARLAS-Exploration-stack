volumes:
# Elasticsearch
  arlas-data-es:
    name: arlas-data-es
  arlas-data-es-logs:
    name: arlas-data-es-logs
  arlas-es-certs:
    name: arlas-es-certs
  arlas-es-config:
    name: arlas-es-config

# Persistence
  arlas-persist:
    name: arlas-persist

# Postgres
  arlas-postgres:
    name: arlas-postgres