{{- if .Values.deployment.arlas.services.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: arlas-permissions-ingress
  annotations:
    {{ .Values.deployment.arlas.services.ingress.annotations | toYaml | nindent 4 }}
spec:
  ingressClassName: {{ .Values.global.ingressClassName }}
  rules:
    - host: {{ .Values.global.dnsDomain }}
      http:
        paths:
          - path: /permissions/
            pathType: Prefix
            backend:
              service:
                name: {{ (get .Values "arlas-services").services.permissions.serviceName }}
                port:
                  number: {{ (get .Values "arlas-services").services.servicePort }}
{{- end }}
