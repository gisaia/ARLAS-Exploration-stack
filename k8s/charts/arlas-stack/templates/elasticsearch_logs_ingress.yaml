{{- if .Values.deployment.elasticsearch.logs.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: elasticsearch-ingress-logs
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: 100240m
    nginx.ingress.kubernetes.io/ssl-passthrough: "true"
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    blackbox.monitoring/enabled: "true"
spec:
  ingressClassName: {{ .Values.global.ingressClassName }}
  rules:
    - host: {{ .Values.global.elasticLogsDnsDomain }}
      http:
        paths:
          - backend:
              service:
                name: arlas-stack-elasticsearch-logs
                port:
                  number: 9200
            path: /
            pathType: ImplementationSpecific
{{- end }}
