{{- if .Values.deployment.arlas.uis.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: arlas-hub-ingress
  annotations:
    {{ .Values.deployment.arlas.uis.ingress.annotations | toYaml | nindent 4 }}
spec:
  ingressClassName: {{ .Values.global.ingressClassName }}
  rules:
    - host: {{ .Values.global.dnsDomain }}
      http:
        paths:
          - path: /hub/?(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ (get .Values "arlas-uis").uis.hub.serviceName }}
                port:
                  number: {{ (get .Values "arlas-uis").uis.servicePort }}
{{- end }}
