
{{- if and .Values.deployment.aias.services.titiler.gateway.enabled .Values.global.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: arlas-titiler-private-route
spec:
  parentRefs:
    - name: {{ .Values.global.gateway.name }}
      sectionName: https
  hostnames:
    - {{ .Values.global.dnsDomain | quote }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /cog/
      backendRefs:
        - name: {{ .Values.deployment.aias.services.titiler.serviceName }}
          port: {{ .Values.deployment.aias.services.titiler.port }}
{{- end }}