apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: arlas-persistence-file-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.storageSize }}
